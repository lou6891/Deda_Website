import React from 'react';
import classes from '../../Pages.module.css'
import {mainPadding} from "../../../Components/Global_variables"

import Cart_Sections from '../../../Components/Layout_elements/Cards_types/Cart_Sections'

function CollectionVolume () {

    /*
    return  (
        <section id='Volume' className={classes.componentsContainer}>
            <Cart_Sections>
                <main className={classes.main} style={{padding : fullWidth ? mainPadding[0] : mainPadding[1] }}>

                    <h2 className={classes.h2}>
                        Collection Volume
                    </h2>

                    <p className={classes.p}>
                        Volume chart
                        <br />
                        Total volume
                        <br />

                    </p>


                </main>
            </Cart_Sections>
        </section>
    )

     */
}

export default CollectionVolume